# Core web framework (tested together)
fastapi==0.110.2
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Data validation
pydantic==2.7.4

# AI libraries (prefer stable google-generativeai API)
google-generativeai==0.8.3
# Keep google-genai for forward compatibility, but code defaults to generativeai
google-genai==0.3.0

# Data processing (compatible with TensorFlow 2.15.0)
numpy>=1.23.5,<2.0.0
pandas>=2.2.0,<3.0.0

# Inference runtime + image handling (use TensorFlow CPU on Windows)
tensorflow==2.15.0
Pillow==10.3.0

# Financial data
yfinance==0.2.40

# HTTP requests
requests==2.32.3

# Environment variables
python-dotenv==1.0.1
